name: sample_run

on:
  pull_request:
    branches:
      - main
      - dev
      - UAT

jobs:
  build-job:
    runs-on: ubuntu-latest

    steps:
      - name: running commands
        run: |
         echo "hello"
         pwd
         uname -r
         date
        
